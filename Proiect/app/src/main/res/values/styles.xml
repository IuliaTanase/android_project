<resources>
    <!-- Light application theme. -->
    <style name="AndroideleStyleLight" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@drawable/app_background</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="fontFamily">@font/coming_soon</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Dark application theme. -->
    <style name="MyDarkTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorBlack</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@drawable/black_background</item>
        <item name="android:textColor">#E8DAD0B1</item>
        <item name="android:iconTint">#E8DAD0B1</item>
        <item name="fontFamily">@font/coming_soon</item>
        <item name="android:textStyle">bold</item>

    </style>

    <style name="AndroideleStyleLight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>